allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  sharp: true

# Netlify's dev tooling (@netlify/images -> ipx) still pins sharp ^0.34, which has
# open libvips/libheif advisories. Astro 7 itself requires >=0.35.4, so dedupe on that.
overrides:
  sharp: '>=0.35.4'
